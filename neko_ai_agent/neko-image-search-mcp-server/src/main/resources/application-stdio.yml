spring:
  ai:
    mcp:
      server:
        name: neko-image-search-mcp-server
        version: 0.0.1
        type: SYNC # 同步调用
        # stdio
        stdio: true
  # stdio
  main:
    web-application-type: none
    banner-mode: off